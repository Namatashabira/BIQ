{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn admin_panel.wsgi:application",
    "healthcheckPath": "/api/core/health/",
    "restartPolicyType": "ON_FAILURE"
  }
}
